# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

The **AIOS** backend: a FastAPI API + Celery workers for a cloud SEO-automation platform (Xegents AI). It lives in a monorepo (`danyals-aios/`) alongside a Next.js `frontend/` and a nested, standalone SEO audit engine at `../danyals-audit-system/` (which has its own `CLAUDE.md`). This service is being built **foundation-first** ("Part 1"): a runnable, tested skeleton before any business logic (auth, DB tables, the module endpoints) lands.

Responsibilities (from `README.md`, mostly still to build): REST/JSON API for `../frontend`; orchestrates the SEO modules (Audit, Content, Off-page, Portal, Policy Radar) via a Celery+Redis job queue; talks to the audit engine, Claude, Serper, Google, and Google Sheets; auth + a per-client encrypted key vault + tier/role enforcement; per-client API budget caps + a daily spend-stop.

## Commands

Everything runs through the local venv at `backend/.venv` (Windows + Git Bash). Always invoke tools as `./.venv/Scripts/python -m <tool>` — do not use bare `python`.

```bash
./.venv/Scripts/python -m pip install -e ".[dev]"          # install (editable) + dev extras
./.venv/Scripts/python -m ruff check .                      # lint
./.venv/Scripts/python -m mypy app workers                  # type-check (strict)
./.venv/Scripts/python -m pytest -m unit -q                 # unit tests (NO external services)
./.venv/Scripts/python -m pytest -m integration -q          # integration tests (need Redis + local Postgres)
./.venv/Scripts/python -m pytest tests/test_config.py::test_secrets_are_masked   # a single test
./.venv/Scripts/python -m uvicorn app.main:app --reload     # run the API (http://localhost:8000)
./.venv/Scripts/python -m celery -A workers.celery_app worker -l info   # run a worker (needs Redis up)
cp .env.example .env                                         # one-time: required for /health/ready
```

The full local gate before committing a change is: **`ruff check .` && `mypy app workers` && `pytest -m unit`** (all green; the unit subset needs no Redis/Postgres). CI at `../.github/workflows/backend-ci.yml` runs ruff + mypy + unit tests on 3.11/3.12 plus a Redis-service integration job.

**Deployment is native (no Docker):** production runs on a single VPS via systemd — `aios-api` (uvicorn) + `aios-worker` (celery) in front of a native Redis, behind Caddy (auto-TLS). Provision with `sudo bash ../infra/deploy/install.sh`; see `../infra/deploy/README-deploy.md`.

## Architecture (big picture)

- **`app/`** — the FastAPI app. `app/main.py` exposes a `create_app()` factory and a module-level `app`. A **lifespan** owns shared clients on `app.state`: a shared `httpx.AsyncClient` and the `redis.asyncio` client (constructed lazily at startup, closed at shutdown behind a `getattr` guard). Middleware is added in the order TrustedHost → CORS → RequestID, which (Starlette adds LIFO) makes **RequestID outermost**. Business routes mount under **`/api/v1`** via the `api_v1` aggregator in `app/routers/__init__.py` — one router per module; health lives at the root (`/health`, `/health/ready`).
- **`workers/`** — Celery app (`workers/celery_app.py`) with broker + result backend on Redis. The long-running Audit/Content/Off-page/Research jobs live here in later parts.
- **`integrations/`** — external API clients (Serper, Google, Google Sheets, Claude), added in later parts.
- **`app/config.py`** — `Settings` (pydantic-settings, reads `.env`), `@lru_cache get_settings()`, and `validate_settings()`. Secrets are `SecretStr`. **`app/logging_setup.py`** — structlog (console in dev, JSON in prod), request-id propagated via `structlog.contextvars`.
- **`app/core/`** — cross-cutting concerns: `security.py` (SSRF guard), `middleware.py` (request-id), `errors.py` (error envelope), `observability.py` (Sentry), `deps.py` (FastAPI dependencies), `redis.py` (async Redis).
- **`app/db/database.py`** — the two local-Postgres (psycopg) connection seams (see invariants): `rls_connection(user_id)` (role `authenticated`, RLS applies) + `privileged_connection()` (role `service_role`, BYPASSRLS) + the async `db_ping` readiness.

**The load-bearing data decision:** a self-hosted **PostgreSQL 16** (managed via ordered SQL migrations in `../db/migrations/`) holds identity, secrets, and the knowledge base — accessed through the two psycopg seams above, with RLS as the tenant boundary; **Google Sheets** holds the client-facing operational records; **Redis** is the job queue + cache + a Sheets write-buffer. See `../context/ARCHITECTURE-AND-PLAN.md` and the `../docs/deliverables/*.pdf` for the full product plan. The frontend's `../frontend/lib/*.ts` types are the intended **API response shapes** — build endpoints to match them so the dashboard works unchanged.

## Invariants (the "why" — read before changing these)

1. **Liveness ≠ readiness.** `GET /health` (liveness) touches no external service. `GET /health/ready` (readiness) checks local Postgres + Redis with **bounded, concurrent** timeouts (`asyncio.gather` + per-check `wait_for`, budget = `settings.readiness_timeout_seconds`); a `not_configured` pool does not fail readiness.
2. **Never block the event loop.** Async routes use `redis.asyncio` and async `httpx`; psycopg is sync, so DB seams are offloaded with `asyncio.to_thread`. The SSRF guard's `socket.getaddrinfo` **blocks** — callers on async routes must offload it too.
3. **Trust-level DB seams / RLS hygiene.** The `service_role` DSN **bypasses Row-Level Security** → server-only, never returned to a client, never logged (`privileged_connection`). RLS-respecting reads/writes flow through **`rls_connection(user_id)`** — role `authenticated`, the verified server-side identity bound transaction-local via `set_config('app.user_id', <sub>, true)` (never a raw client string, never string-formatted → impersonation via a bound value is impossible). service_role bypasses **policies, not triggers** (task-guard writes must use the RLS seam).
4. **12-factor config.** Env-only; `@lru_cache` singleton; `validate_settings` **fails fast in prod** on a missing required secret and **warns in dev** (checks falsiness, since a blank env var is `""`/`SecretStr("")`, not `None`). `APP_ENV` drives docs, CORS, and log format; docs/openapi are disabled in prod.
5. **Observability.** Every request gets an `X-Request-ID` stored on **`request.state`** (so it survives the 500 path, after contextvars are cleared); JSON logs in prod; a global error envelope `{"error": {"type", "message", "request_id"}}` (generic 500 message — never leak `str(exc)`); Sentry initializes only if `SENTRY_DSN` is set. Secrets never appear in a log line.
6. **Readiness ping contract.** Every dependency ping is `async def ping(...) -> DependencyStatus` — **non-raising** (catches its own errors), only true success is `"ok"`, and `detail` is a short **sanitized** reason (never a connection URL, secret, or raw exception). `not_configured` does not fail readiness in dev.
7. **Redis DB separation.** App cache/health uses `REDIS_URL` (db 0); Celery uses `CELERY_BROKER_URL` (db 1) + `CELERY_RESULT_BACKEND` (db 2), so a cache `FLUSHDB` can never wipe queued jobs.
8. **Celery long-job safety.** `task_acks_late=True` + `worker_prefetch_multiplier=1`; the Redis broker's `visibility_timeout` **must be ≥ the longest `task_time_limit`**, or a long job is redelivered and **runs twice** (double API spend).
9. **Auth + RBAC (local).** The API mints and verifies its **OWN EdDSA (Ed25519) access tokens** — `POST /api/v1/auth/login` (username/password → argon2id verify against `auth.users`) signs a short-lived token with the private key; every request verifies it against the **static public key** under a hard `["EdDSA"]` allow-list (defeats alg-confusion / `none`) with no network round-trip (`app/core/auth.py`, `app/services/tokens.py`). No JWKS, no GoTrue. The verified `sub` → `CurrentUser` loaded via **`rls_connection`**; there is **no public signup** — `provision_user(...)` (keyword-only) writes the `auth.users` credential + `public.users` identity in one privileged transaction. The **17-feature matrix + 6 roles + 4 templates** live as versioned reference data in `app/rbac/matrix.py` (mirrored from `frontend/lib/data.ts`) — enforcement (`require_perm` / `require_role` / `require_owner` / `require_feature`) needs no DB round-trip; **Owner is all-on and locked**.
10. **Every tenant table has `ENABLE`+`FORCE RLS`** with explicit policies; a CI gate (`app/db/rls_check.py`, the `db-rls` job) fails on any unprotected `public` table. RLS helper functions are `SECURITY DEFINER` with an empty `search_path` to avoid policy recursion. **Vault** raw secrets are sealed app-layer with AES-256-GCM (`app/services/vault.py`) under `VAULT_MASTER_KEY` (env-only, NEVER in Postgres — rotating it requires re-sealing existing secrets, `key_version` supports it); the `secret_sealed` bytea + masked metadata is written/read on `privileged_connection`; a list is masked, reveal is owner-only, nothing is logged. The **activity log** is append-only (no write policy; only the server appends). The **cost-gate** (`app/services/cost_gate.py`) runs `dial → cache → client cap → daily spend-stop → call+log` before any paid call; a cached call costs 0.

## Conventions

- Tooling: **ruff** (line-length 110), **mypy strict** (`ignore_missing_imports`), **pytest** (`asyncio_mode=auto`, markers `unit`/`integration`). Mirror the sibling audit engine's Python style.
- `from __future__ import annotations` at the top of every module.
- **Reuse before rewriting.** `app/core/security.py` is ported verbatim from `../danyals-audit-system/audit_engine/security.py`; the async HTTP-client base in that repo's `integrations/base.py` (tenacity retry + circuit breaker + never-log-secrets) is the template for future API clients. The audit engine is later wrapped as a Celery job — invoke its CLI, don't rebuild it; note it **mints its own `run_uuid`** and **doesn't catch its own top-level exceptions**, so the worker must own timeouts and mark failures itself.

## Context discipline (keep the main thread lean)

The main conversation is a coordinator, not a workspace. Push reading, searching, and bulk work off it so its token budget stays low and long tasks don't hit the context limit. Prefer these over `/compact` (which is lossy) and never let the main thread fill with raw file dumps.

1. **Delegate reading and searching to subagents.** When answering means sweeping many files or exploring a subsystem, spawn a subagent (Task/Agent, or the `Explore`/`general-purpose` types). It reads in *its own* context and returns only the conclusion — the main thread grows by a paragraph, not 50k tokens. This is the single biggest lever.
2. **Externalize state to files, reference by path.** Write plans, findings, and intermediate results to the scratchpad dir or a repo doc, then point back at the path instead of pasting the content inline. Durable facts go to memory (`MEMORY.md` + memory files), which load as a compact index, not full bodies.
3. **Read narrowly.** Grep/Glob to locate first, then Read only the needed line range (`offset`/`limit`). Don't read whole files when a slice answers the question, and don't re-read a file you just edited — the harness already tracks its state.
4. **Session hygiene.** One session per distinct task; `/clear` when a task is done (save anything important to files/memory first); start fresh rather than growing one giant thread. Use `/compact` with a focus instruction only as a last resort.
5. **Keep this file and other always-loaded docs lean.** CLAUDE.md is re-read every turn — every line here is fixed overhead on every request. Add durable operating rules; move transient notes to the scratchpad or memory.

## Build state

Built in ordered "chunks" on branch **`feat/backend-foundation`**, one commit per chunk (`feat(backend): <desc> (Chunk N)`). **Part 1 (the runnable foundation) is complete — Chunks 1–10 committed and green** (ruff + mypy strict clean; 51 unit tests pass with no external services):

- 1–3: packaging, config + structlog logging, app factory + middleware + liveness `/health`.
- 4: SSRF guard (`app/core/security.py`) ported verbatim from the audit engine, with an async caller-contract docstring.
- 5: Supabase seams (`app/db/supabase.py`) — `get_admin_client` (service-role, cached, server-only) + `client_for_user` (anon+JWT, per-request, never cached) + async readiness ping.
- 6: shared `redis.asyncio` client (`app/core/redis.py`) + `RedisDep` + lifespan wiring + readiness ping.
- 7: concurrent, bounded `/health/ready` (`asyncio.gather`, 503 via `response.status_code` not `HTTPException`, `not_configured` doesn't fail readiness).
- 8: Celery skeleton (`workers/celery_app.py` + `workers/tasks/ping.py`), tasks via `include=[...]`, `visibility_timeout ≥ task_time_limit`.
- 9: **native systemd deploy (Docker was built then removed)** — `infra/systemd/*.service` + `infra/deploy/install.sh` + `README-deploy.md`; Caddy for TLS.
- 10: CI at `../.github/workflows/backend-ci.yml` (ruff + mypy + tests, matrix 3.11/3.12, Redis integration job) + README.

**Part 2 (the Shared Base) is complete — P2-1…P2-10 committed and green** (ruff + mypy strict clean; 136 unit tests, integration auto-skip). DB schema is ordered SQL in `../db/migrations/` (snapshot `../db/schema.sql`); apply with `psql`, then verify RLS with `python -m app.db.rls_check` (needs `DATABASE_URL`). CI's `db-rls` job spins an ephemeral Postgres, applies the migrations (`0000_local_platform.sql` provisions the substrate — no Supabase shim), and runs the gate.

- P2-1: migration tooling + conventions + the RLS coverage gate (`app/db/rls_check.py`).
- P2-2: identity (`users` ↔ `auth.users`) + `user_feature_grants`; the RBAC matrix (`app/rbac/matrix.py`).
- P2-3: auth (`app/core/auth.py` — token verify, `CurrentUser`, `require_*`) + super-admin provisioning + `/api/v1/rbac/*` reference endpoints. (Auth was JWKS/Supabase in Part 2; **Part 6 replaced it with local EdDSA** — see invariant #9.)
- P2-4: clients + sites CRUD (`app/routers/clients.py`, `app/db/clients_repo.py`) — the portal password is never persisted/revealed.
- P2-5: Key Vault (`app/services/vault.py`, `app/routers/vault.py`) over Supabase Vault — masked list, owner-only reveal.
- P2-6: append-only activity log (`app/services/activity.py`) wired into every mutation; `/api/v1/activity` feed.
- P2-7: cost-gate/money-dial (`app/services/cost_gate.py` + `/api/v1/cost/*`) — budgets, dial, spend-stop, cost log.
- P2-8: service tiers (`app/routers/tiers.py`) — delivery tier (free/semi/fully) kept separate from the subscription tier.
- P2-9: E2E + RLS integration tests (auto-skip). P2-10: harden + docs.

Business routers attach to `api_v1` in `app/routers/__init__.py`; each has a `*_repo.py` (RLS reads via `rls_connection`, swappable for an in-memory fake in tests) and, where privileged, a service using `privileged_connection` (service_role). Integration tests (`-m integration`) need a real Redis + local Postgres and auto-skip when their env var is unset.

**Part 3 — Module 01 Audit** is in progress; P3-1…P3-6 committed and green (ruff + mypy strict; 177 unit tests, integration auto-skips). A provisioned user triggers a URL-only audit (Free/Paid) from the API; a Celery worker runs the **external** engine and owns the run's lifecycle.

- P3-1: `0008_audits.sql` job-ledger table (`audit_tier`/`audit_status` enums; run_uuid + artifact refs + scores jsonb + cost; ENABLE+FORCE RLS, passes the gate). `app/schemas/audits.py` mirrors `lib/audit.ts` (`AuditRow`). Settings `AUDIT_ENGINE_DIR`/`AUDIT_ENGINE_PYTHON` (+ timeout/max_pages/profile/cost-estimate/artifact-dir).
- P3-2: `integrations/audit_engine.py` — the engine is a **separate product** invoked as a subprocess with **its own interpreter** (never imported here; dependency clash). It mints its own `run_uuid` and never times out or catches its own errors, so the adapter owns a hard timeout (< the Celery `task_time_limit`), parses `run_uuid` from stdout (`COLUMNS=1000` defeats rich's 80-col wrap), reconstructs the artifact dir from the slug+uuid, reads `run.json` `scores.overall`, and returns a typed result — a timeout/crash/non-zero-exit is a typed failure, never a half-owned run.
- P3-3: `workers/tasks/audit.py` drives queued→running→done|failed; **never leaves a job "running"**; **never re-raises** (with `task_acks_late` that would redeliver and double-spend); redelivery of a `done` row is a no-op. Logs the run cost via the Part-2 path (Free = $0, Paid = estimate; the engine reports no spend). DB/cost writes go through an injected store (service_role) so the core is unit-tested with a fake store + mocked runner.
- P3-4: `routers/audits.py` — `POST /audits` (`require_perm("run_audits")`, SSRF-guard off the loop, Free-tier gating of paid types, enqueue, return the queued `AuditRow`), `GET /audits` + `/audits/{id}` + `/audits/stats`.
- P3-5: `app/services/audit_artifacts.py` copies the run's report PDF + `findings.json` into a controlled root (traversal-safe); guarded `GET /audits/{id}/report.pdf` + `/findings.json` downloads; the ArtifactStore seam lets Supabase-Storage/signed-URLs slot in later.
- P3-6: live integration test (`tests/integration/test_audit_engine_live.py`, auto-skips unless the engine env exists) runs one real Free audit end-to-end.

**Engine reality (source-verified, differs from first assumptions):** the `full` CLI prints only `Run UUID:` + `Artifact dir:` (no DB run id); it writes `report-consolidated.pdf`/`report-full.pdf`/… (NOT `report-final.pdf`); `run.json` carries `scores.overall` + `on_page/technical/off_page/local` (no `geo`/`backlink` aggregate); and it emits **no** machine-readable spend (Paid cost is the configured estimate). The engine needs its own venv + `.env` — until then the live test skips and the module runs fully mocked.

**Part 4 — Client + Admin Audit Flow** is complete; P4-1…P4-6 committed and green (ruff + mypy strict; **210 unit tests**, integration auto-skips without env). Adds the **client trust boundary**: a provisioned portal `client` logs in, sees only its OWN tenant, runs/downloads only its OWN audits, and is **provably unable** to reach another tenant's data — proven not just via FastAPI 404 but via **direct PostgREST with the client's JWT** (finding 11).

- P4-1: `0009_app_role_client.sql` adds a 7th `app_role` label `client` in its **own committed migration** (PG forbids using a new enum label in the txn that adds it — 55P04). `0010_client_portal.sql` adds `users.client_id` (+ CHECK: set iff role='client'), **redefines `is_staff()` to EXCLUDE clients** (so every prior staff-scoped base-table policy default-denies a client) + `current_client_id()`, and the **three security-barrier views** `portal_audits`/`portal_client`/`portal_sites` (safe column subset, self-filtered by `current_client_id()`, owned by a BYPASSRLS role, `security_invoker` left default). **No client SELECT policy on any base table** — the views are the entire client read surface (the anon-key + JWT can hit PostgREST directly, so RLS is the only boundary). Applied to Supabase; RLS gate green (views correctly not flagged — `rls_check` filters to `relkind='r'`).
- P4-2: `app/rbac/matrix.py` adds `UserRole` (7 roles), `STAFF_ROLES`, `is_staff_role`; `role_has_perm`/`perms_for_role`/`feature_allows` **early-return for `client`** (no staff perm/feature). `CurrentUser.client_id` (server-trusted, never from a body) + `CurrentClient`/`get_current_client`/`CurrentClientDep` (403 unless role='client' with a client_id).
- P4-3: `provision_user` accepts `role='client'` + `client_id` (validated to mirror the DB CHECK); owner-only `POST /clients/{id}/portal-users` mints a portal login with the role fixed and the tenant pinned from the PATH; `admin_users._fetch_all_users` adds `.neq('role','client')` so clients never appear on the Team screen.
- P4-4: `app/db/portal_repo.py` reads the `portal_*` views (RLS-scoped); `app/services/client_audits.py` `create_client_audit` pins `client_id` from `CurrentClient`, gates Paid behind `delivery_tier<>'free'` (D5), SSRF-guards off-loop, inserts via the **service_role** admin client (D6 — clients have no base-table SELECT to read a user-JWT insert back), enqueues, logs activity. `PortalAuditCreate {url,tier,types}` has **no client_id field**; `PortalAuditResponse` exposes only safe columns.
- P4-5: `app/routers/portal.py` (all routes behind `CurrentClientDep`): `GET /portal/dashboard`, `/portal/audits`, `/portal/audits/{id}` (own else 404), `/portal/audits/{id}/report.pdf` + `/findings.json` (ownership verified via the view, path resolved server-side via admin, file served — **path never returned**), `POST /portal/audits`. Staff `GET /audits*` now `require_perm("view_reports")` — all 6 staff roles hold it, `client` does not → clients 403'd out of the staff namespace (D10).
- P4-6: `tests/integration/test_portal_isolation.py` (auto-skips without the local DB DSNs) provisions 2 client tenants + 1 staff and proves, **via each client's identity on the `authenticated` role hitting the tables directly** (`rls_connection`, i.e. a leaked portal DB credential; rewritten from PostgREST-with-JWT in Part 6), that base tables return 0 rows and `mrr/cost/error/*_path` are unreachable, the view shows only own audits, a Free run pins `client_id`, `delivery_tier='free'` blocks Paid, staff still see all, and clients are absent from the roster. **Verified live against local Postgres (Part 6): passes.**

**Part 5 — Team Flow (task queue · board · review checkpoint)** is complete; P5-1…P5-5 committed and green (ruff + mypy strict; **244 unit tests**, integration passes live / audit-engine test auto-skips). A signed-in staff member sees their queue and advances tasks through the lifecycle; leads (owner/admin/manager = the `assign_tasks` holders) assign/route work and sign off the content review gate. The lifecycle + review checkpoint are enforced **at the DB**, not just FastAPI.

- P5-1: `0011_tasks.sql` — `task_type`/`task_priority`/`task_status` enums, `tasks_code_seq` (the public `J-####` code, never a UUID), `public.tasks` table + indexes, ENABLE+FORCE RLS (select=`is_staff`; insert=owner/admin/manager; update=`assignee_id=auth.uid()` OR lead — the actor guard). **THE BOUNDARY (finding, mirrors 0010's threat model):** staff hold the public anon key + a JWT and can PATCH PostgREST directly, so the state machine can't live only in FastAPI. `tasks_guard_update()` (BEFORE UPDATE, SECURITY DEFINER, empty `search_path`) enforces it: a non-lead may change **only** `status` and only along a legal transition (todo→in_progress; in_progress→review iff content_sprint; in_progress→done otherwise) — so **leaving `review` (sign-off/reject) and skipping review are lead-only**; `tasks_guard_insert()` rejects a client-role assignee. Applied to Supabase; RLS gate green (12 tables).
- P5-2: `app/schemas/tasks.py` (`TaskResponse` in the exact frontend `Task` shape — `id`=public code, no internal column leaks; `next_status` mirrors `portal.ts` byte-for-byte; `type_to_db`/`format_due`/`needs_review`) + `app/db/tasks_repo.py` (RLS user-JWT reads + insert + `update_task_by_code` with an `expect_status` gate for optimistic concurrency).
- P5-3: `app/routers/tasks.py` — `GET /tasks` (`view_reports`; `mine`/`assignee` scoping), `POST /tasks` (`assign_tasks`; validates client + staff assignee), `POST /tasks/{code}/advance` (assignee-or-lead; content_sprint→review else →done; review/done→409; optimistic 409), `POST /tasks/{code}/review` (owner/admin/manager; approve→done, reject→in_progress; 409 unless review), `PATCH /tasks/{code}` (`assign_tasks`). Every mutation records an activity entry (`kind=content` for content_sprint, else `task`).
- P5-4: `app/routers/me.py` — `GET /me` returns the caller's `TeamMemberRecord` with LIVE `activeTasks`/`completed` from the tasks ledger; onTime/utilization/quality stay at defaults (Part 6).
- P5-5: `tests/integration/test_tasks_flow.py` (auto-skips without the local DB DSNs) proves, **via each principal's identity on the `authenticated` role hitting the tables directly** (`rls_connection`, rewritten from PostgREST-with-JWT in Part 6), that a non-lead status→done UPDATE is REJECTED by the trigger (skip-review jump AND review self-sign-off), a non-status column edit is REJECTED, a lead CAN sign off review→done and reject review→in_progress, an assignee must be staff (reassign→client uid rejected), my-queue scoping works, and a portal client is fully excluded. **Verified live against local Postgres (Part 6): passes.**

**Elevation (post-audit hardening, 8.1→~9.4; branch `feat/backend-foundation`).** Raised the verification/perf/observability gaps without regressing the 9s. New standing guarantees + invariants:
- **`tests/integration/test_route_contracts.py`** — the E2E HTTP contract suite: drives the REAL app (`create_app()` + `LifespanManager`) over `ASGITransport` with REAL Supabase JWTs through the actual dep graph (the class that hid the empty-JWT bug), asserting status + response SHAPE for all 51 ops × roles. PROVEN to fail on pre-e53fc05 code. Its shape helper derives keys from the response_model; a bounded **retry only on a transient 5xx** absorbs free-tier blips without masking deterministic bugs.
- **`tests/integration/test_rls_matrix.py`** — systematic RLS correctness (role × table × op allow/deny vs a policy oracle mirrored from live `pg_policies`; cross-tenant 0-rows; sensitive columns unreachable). Reads retry only on a transport error (RLS decisions never raise).
- **`tests/test_contract_lock.py`** — build-time lock: each response model's emitted keys must equal its `frontend/lib/*.ts` type (fails on drift).
- **Perf (`docs/PERF-BASELINE.md`):** `client_for_user()` was ~307ms/req (SSL context rebuilt per client); now shares one `ssl` context via `SyncClientOptions.httpx_client` → **~0.9ms** (RLS unchanged, proven by the matrix). `app/core/pagination.py` `PageDep` (limit 1-200/offset) caps every DB-backed list endpoint via `.range()`; aggregation callers (`/audits/stats`, `/portal/dashboard`) stay unbounded.
- **Observability:** `app/core/metrics.py` `MetricsMiddleware` (added INSIDE RequestID so that invariant holds) + `/metrics`; `infra/alerts/backend-alerts.yml` (Backend5xxSpike pages on the empty-JWT class); `ErrorCode` taxonomy in `errors.py`; `app/core/ratelimit.py` (per-user, fail-OPEN) on the audit-create mutations.
- **Test strength:** `tests/mutation/run_mutation.py` (stdlib AST mutation tester; mutmut has no native Windows) — `rbac/matrix.py` 100%, `schemas/tasks.py` 90%. **281 unit green.**
- CI (`../.github/workflows/backend-ci.yml`): added a `security` job (pip-audit + gitleaks) and wired `TEST_SUPABASE_*` secrets so the live suites run in CI when a test project is provided (else auto-skip). Full elevation writeup: `docs/ELEVATION-REPORT.md`.

**Part 6 — self-hosted Postgres migration (P6A).** Supabase is **removed entirely**; the whole runtime is on a self-hosted **PostgreSQL 16** with local auth. The data plane is the two psycopg seams (`rls_connection` / `privileged_connection`, invariant #3); the vault seals app-layer AES-256-GCM under `VAULT_MASTER_KEY` (invariant #10); auth mints + verifies its **own EdDSA tokens** (`POST /api/v1/auth/login`, invariant #9), and `provision_user(...)` (keyword-only) writes local `auth.users`+`public.users`. **P6A-8** deleted `app/db/supabase.py` and the `supabase` dependency, dropped the `SUPABASE_*` settings (prod now requires `database_url`/`database_admin_url`/`jwt_private_key`/`jwt_public_key`/`vault_master_key`), reduced readiness to Postgres + Redis, and **rewrote the 6 superseded security suites onto local Postgres**: `test_portal_isolation` + `test_tasks_flow` + `test_rls_matrix` prove the tenant/trigger boundary via the **direct authenticated-role probe** (`rls_connection(uid)` = a leaked portal/staff DB credential; every original assertion preserved), and `test_route_contracts` + `test_route_auth_wiring` + `test_local_e2e` drive the real app with **locally minted EdDSA tokens**. Fixed a latent bug the newly-live contract suite surfaced: `advance_task` compared a psycopg `uuid.UUID` assignee to a `str` id (always ≠) → the assignee was wrongly 403'd; now string-normalized. Full integration suite: **58 passed, 1 skipped** (audit-engine-live only).

**Deferred (needs the user):** live **Paid** audit execution + Google Sheets/Reports stay parked until Google/provider keys land — Paid gating/enqueue/row-creation are built + unit-tested now with the worker mocked. The team **portal frontend has no fetch layer yet** (`PortalContext` runs on `tasks_seed` + local mutations); Part 5 ships the correctly-shaped API — a separate frontend wiring pass is still required. **Redis is now running on this box** (localhost:6379); Redis integration tests pass. The portal login flow itself needs no new keys.
