# context

Engineering context for the team and for AI assistants. Read these to
understand what the platform is and how it should be built, before touching code.

| File | What it covers |
|---|---|
| `ARCHITECTURE-AND-PLAN.md` | The system architecture and build plan. Start here. |
| `AIOS-API-Research.pdf` | External APIs the platform uses, their cost, and provider choices. |
| `AIOS-Data-Flow-Structure.pdf` | How data moves: URL in, gather, score, store, render. |
| `AIOS-Workflow-and-Features.pdf` | The v1 feature map and per-module workflows (Free / Paid tiers). |

The **audit engine** and its own API keys are documented separately in
`../../danyals-audit-system` (outside this repo).

Rendered, client-facing versions of some of this material live in
`../docs/deliverables/`.
